# FIELDNOTES Editor

Public editor and shared renderer for FIELDNOTES documents.

Remote images are disabled by default in the editor. Embedded clients may opt in with the editor's `allowRemoteImages` option; only HTTPS image URLs are accepted, while other network access remains blocked by the content policy.
